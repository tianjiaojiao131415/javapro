package Day_0213;import java.util.ArrayList;import java.util.List;import java.util.Random;import java.util.Scanner;/** * Created by admin on 2020/2/13. */public class LangRenSha {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        System.out.println("请输入游戏人数（12—18人）");        int players =scanner.nextInt();        List <String> all = new ArrayList<>();        List <String> player = new ArrayList<>();        List <String> dipai = new ArrayList<>();        //判断是否符合游戏的人数        if (players>18||players<12){            System.out.println("不符合游戏人数");        }else{            initePai(all);            addPai(all,players);            all.remove("盗贼");            initDiPai(all,dipai);            all.add("盗贼");            System.out.println(all);            System.out.println(dipai);        }    }    //初始化12人的游戏人的牌数    public  static void initePai(List<String> all){        for (int i = 0; i < 4; i++) {            all.add("狼人");        }        for (int i = 0; i <4; i++) {            all.add("村民");        }        all.add("预言家");        all.add("女巫");        all.add("丘比特");        all.add("守护 ");        all.add("猎人");        all.add("村长");        all.add("盗贼");    }    //当超出了12个人时，根据不同的人数添加不同的角色    public static void addPai(List<String> all ,int players){            if (players>12)                all.add("村民");            if (players>13)                all.add("替罪羊");            if (players>14)                all.add("狼人");            if (players>15)                all.add("村民");            if (players>16)                all.add("村民");            if (players>17)                all.add("吹笛者");    }    public static void initDiPai(List<String> all,List<String> dipai){        while (dipai.size()<3){            Random random = new Random();            //随机从所有的牌中选出3张，放入到底牌中            int di = random.nextInt(all.size());            //判断底牌是否含有狼人，狼人的人数不能超过两个,如果有，就继续循环            if (dipai.contains("狼人")&&dipai.get(di).equals("狼人")){                continue;            }else{                //把随机取到的底牌重新加到底牌的数组中                dipai.add(all.remove(di));            }        }    }}