package Days_0212;/** * Created by admin on 2020/2/12. */public class MyThread extends Thread {    @Override    //如果用 继承Thread类或者实现接口Runnable接口的方式，那么必须要重写run方法    public void run() {        for (int i = 0; i <10 ; i++) {            try {                //传一个毫秒值，500毫秒内放弃对cpu的抢占，500毫秒之后会开始对cpu的抢占，但是不一定可以强到                Thread.sleep(500);            } catch (InterruptedException e) {                e.printStackTrace();            }            System.out.println(Thread.currentThread().getName()+"线程输出------"+i);//Thread-0线程输出------6        }    }}