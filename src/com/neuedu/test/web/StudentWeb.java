package com.neuedu.test.web;import com.neuedu.test.pojo.Student;import com.neuedu.test.service.IsStudentService;import com.neuedu.test.service.StudentService;import java.util.List;import java.util.Scanner;/** * Created by admin on 2020/2/18. */public class StudentWeb {    //创建一个StudentService的对象    IsStudentService studentService = new StudentService();    public void ShowMain(){        System.out.println("1------------------查询");        System.out.println("2------------------添加");        System.out.println("3------------------修改");        System.out.println("4------------------删除");        System.out.println("5------------------退出");        System.out.println("=========================");    }    public void input(){        Scanner scanner = new Scanner(System.in);        int i = scanner.nextInt();        if (i==1){           Query();        }else if (i==2){           add(scanner);        }else if (i==3){           Update(scanner);        }else if (i==4){           Del(scanner);        }else {            System.exit(0);        }    }    //添加方法    public void Query(){        //在此需要调用service层的方法        List<Student> list = studentService.Query();        for (Student student:list ){            System.out.println(student);        }    }    public void add(Scanner scanner){        System.out.println("请输入要添加的名字");        String name = scanner.next();        System.out.println("请输入要添加的年龄");        int age = scanner.nextInt();        System.out.println("请输入要添加的性别");        int garden = scanner.nextInt();        //创建一个新的学生对象        Student student = new Student(name,age,garden);        studentService.add(student);        //显示全部        Query();    }    public void Update(Scanner scanner){        System.out.println("请输入要修改的id");        int id = scanner.nextInt();        System.out.println("请输入要修改的名字");        String name = scanner.next();        System.out.println("请输入要修改的年龄");        int age = scanner.nextInt();        System.out.println("请输入要修改的性别");        int garden = scanner.nextInt();        //把值放入到student对象中        Student student = new Student(id,name,age,garden);        studentService.Update(student);        Query();    }    public void Del(Scanner scanner){        System.out.println("请输入你要删除的id");        int id = scanner.nextInt();        studentService.Del(id);        Query();    }}