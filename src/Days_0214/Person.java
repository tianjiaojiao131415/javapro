package Days_0214;/** * Created by admin on 2020/2/15. */public class Person {    private  Integer age ;    private String name;    @Override    public boolean equals(Object obj) {        //增加一个判断，如果传的obj等于this本身，那么直接返回true        if (this==obj){            return true;        }        //增加一个判断，如果传的obj为空，那么直接返回false 提高程序的效率        if (obj==null){            return false;        }        //增加一个判断，如果是Person类型就转换，防止出现类型转换异常ClassCastException      //instanceof 判断是不是，是就执行       if (obj instanceof Person){           //使用向下转型，把Object类型转为Person类型           Person p = (Person)obj;           //比较两个对象的属性，一个是调用方法的this（p1），另一个是p（p2）           boolean b = this.name.equals(p.name) && this.age==p.age;           return b;       }        //如果不是就返回false        return false;    }    public Person() {    }    public Person(Integer age, String name) {        this.age = age;        this.name = name;    }    public String getName() {        return name;    }    public void setName(String name) {        this.name = name;    }    public Integer getAge() {        return age;    }    public void setAge(Integer age) {        this.age = age;    }}